import { colors } from './colors';
import { spacing, lineHeight } from './spacing';
import { size } from './size';
import { fontWeight, fontSize, fontFamily } from './fonts';

export const theme = {
  colors,
  spacing,
  lineHeight,
  size,
  fontWeight,
  fontSize,
  fontFamily,
};
